# ✅ ИСПРАВЛЕНО РОУТИНГ

## Проблема:

Expo Router не мог найти маршруты - ошибка "Unmatched Route".

## Что сделано:

1. ✅ Создан `app/index.tsx` - минимальный маршрут для expo-router
2. ✅ Упрощен `app/_layout.tsx` - убрал Stack, оставил только AppWrapper
3. ✅ Удалена папка `app/(tabs)` - она больше не нужна
4. ✅ Очищены все кеши
5. ✅ Запущен Expo заново

## Теперь:

1. **Дождись QR-кода** в терминале (10-20 секунд)
2. **Отсканируй QR-код** в Expo Go
3. **Приложение должно запуститься** без ошибки "Unmatched Route"

## Важно:

AppWrapper рендерится напрямую в `app/_layout.tsx`, а `app/index.tsx` нужен только для совместимости с expo-router.

**Все исправлено - должно работать!**

