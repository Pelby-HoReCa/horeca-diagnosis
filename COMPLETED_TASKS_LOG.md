# Completed Tasks Log

## 2026-03-03

1. Экран `Профиль`: модальное окно `Настройка уведомлений` приведено к общему UI-паттерну выбора проектов.
   - Переключатели заменены на `radio-active-icon.svg` / `radio-inactive-icon.svg`.
   - Обновлена механика выбора: выбор по всей строке и по индикатору справа.
   - Заголовок модалки: `+2` к размеру, вес шрифта уменьшен на `100`, выравнивание влево.
   - Кнопка закрытия уменьшена и переведена на общий `compact-button-icon.svg`.
   - Заголовки пунктов: `+2` к размеру шрифта.
   - Подпись `Рекомендуем`: `+1` к размеру шрифта.
   - Высота пунктов уменьшена на `6px`, добавлена серая обводка в общем стиле приложения.
   - Нижний отступ от последнего пункта до края модалки увеличен на `10px`.

2. Экран `Профиль`: доп. корректировка модалки `Настройка уведомлений`.
   - Фон пунктов выбора изменен на `#E2E4E9`.
   - Заголовок увеличен еще на `+1` (`22 -> 23`) и поднят выше на `10px`.
   - Крестик выровнен по центру строки заголовка (через layout строки заголовка).

3. Экран `Профиль`: корректировка цветов модалки `Настройка уведомлений`.
   - Цвет фона модалки изменен на `#F6F8FA`.
   - Цвет фона пунктов выбора изменен на `#FFFFFF`.

4. Экран `Профиль`: точечная правка высот и отступов модалки `Настройка уведомлений`.
   - Высота пунктов выбора уменьшена на `8px` для всех пунктов, кроме `Раз в месяц` (`minHeight: 62` через `notificationsOptionCompact`).
   - Высота модалки увеличена на `20px` за счет `paddingTop: 16 -> 36`.
   - Позиция заголовка сохранена (без дополнительного сдвига относительно экрана).

5. Экран `Профиль`: заголовок и крестик модалки `Настройка уведомлений` подняты на `5px`.
   - Применен визуальный сдвиг `transform: translateY(-5)` для строки заголовка.
   - Остальная раскладка модалки не изменялась.

6. Экран `Профиль`: добавлен режим для пользователя без созданного профиля.
   - Если нет данных профиля и заведений, вместо блока ФИО/аватара/контактов/списка заведений показываются пункты:
     `Мои данные` (переход в `EditProfile`) и `Мои заведения` (переход в `AddProject`).
   - Добавлены серые разделители в стиле приложения и иконка плюса справа (как в профиле).
   - Для проверки добавлена тестовая кнопка-переключатель `Тест: пустой профиль` / `Тест: обычный профиль`.
   - Для новых пунктов использован `AnimatedPressable` (анимация нажатия).

7. Экран `Профиль`: корректировки режима «профиль не создан» и модалки уведомлений.
   - Удалена нижняя из двух серых линий под пунктом `Мои заведения`.
   - Заголовки `Мои данные` и `Мои заведения` приведены к стилю секционных заголовков (`Настройки`/`Документы`).
   - При переходе из `Мои данные` открывается `EditProfile` в режиме пустой формы (`startEmpty`), без подгруженной авы.
   - Затемнение фона у модалки уведомлений сделано менее заметным (`animationType: fade` + меньшая непрозрачность фона).

8. Экран `Профиль`: выравнивание заголовков и анимации модалки уведомлений.
   - Добавлена вторая серая линия между `Мои заведения` и `Настройки` с тем же паттерном отступов, что между `Мои данные` и `Мои заведения`.
   - Заголовки `Мои данные` и `Мои заведения` выровнены по левому краю (в один столбец с `Настройки`).
   - Модалка уведомлений возвращена к выезду снизу (кастомный slide карточки), при этом затемнение фона остается мгновенным и мягким.

9. Экран `Профиль`: увеличены отступы между серыми линиями и заголовками `Мои данные` / `Мои заведения`.
   - Высота строк увеличена примерно на `8px` (`minHeight: 52 -> 60`) для более равномерного вертикального ритма.

10. Создан полный локальный бекап проекта.
   - Архив: `../backups/MVP готов.tar.gz`
   - Содержимое: полный снимок папки `horeca-diagnosis`.

11. Экран `Профиль`: удалена тестовая кнопка режима пустого профиля.
   - Убрана кнопка `Тест: пустой профиль` и связанный тестовый стейт/стили.
   - Логика показа пустого профиля теперь только по фактическому отсутствию данных.

12. AI-агент: реализован рабочий backend-эндпоинт и провайдеры LLM.
   - Добавлен `POST /ai/chat` в `backend/src/index.js` с валидацией, историей диалога и таймаутом.
   - Подключены провайдеры: `YandexGPT` (default) и `GigaChat` (fallback/optional), выбор через `AI_PROVIDER`.
   - Обновлен `backend/.env.example` переменными для AI-конфигурации.

13. AI-агент: реализован рабочий экран чата в приложении.
   - Экран `src/screens/AIAssistantScreen.tsx` заменен с заглушки на чат: сообщения, подсказки, ввод и отправка.
   - Добавлен `src/utils/aiService.ts` для вызова backend `/ai/chat` с контекстом пользователя/заведений.
   - Показан источник ответа (`provider`) и обработка ошибок сети/API.

14. AI-агент: визуальная полировка стартового экрана чата под макет.
   - Кнопка `Помощь PELBY` приведена к стилям шапки главного экрана (цвет, размеры, иконка `help-button-icon.svg`).
   - Подключена иконка `Icon (1)` в приветственном блоке; при генерации ответа (`isLoading`) добавлена анимация вращения + пульсации.
   - Подзаголовок `Задайте любой вопрос о вашем бизнесе` приведен к стилю секционных заголовков профиля.
   - Заголовок `Добро пожаловать в AI-агент Pelby` приведен к стилю заголовка экрана `Диагностика`.
   - Блок `ПОДСКАЗКИ`: цвет `#919191`, подсказки выровнены вправо.
   - Кнопки подсказок: фон `#191BDF`, текст `#FFFFFF`, обновлены скругление и размеры.
   - В поле ввода подключены `Voice.svg` (активная) и `Voice — копия.svg` (неактивная) с переключением по факту ввода текста.

15. AI-агент: корректировки по уточнённому ТЗ (режим имитации).
   - Кнопка `Помощь PELBY` перенесена в позицию шапок (`Главная`/`Диагностика`/`Задачи`) по тем же отступам.
   - Цвет текста `Задайте любой вопрос о вашем бизнесе` изменен на `#31353F`.
   - Заголовок `Добро пожаловать в AI-агент Pelby` увеличен на `+3` (`27 -> 30`).
   - Все интеракции экрана переведены в режим имитации нажатия (без бизнес-действий и отправки в AI).
   - Поле `Введите вопрос...`: вес шрифта уменьшен на `100` (`fontWeight: 300`), само поле растянуто на всю ширину экрана.
   - Форма синих подсказок приведена к паттерну `Message green (1)` (срезанный верхний правый угол).
   - Добавлена тестовая кнопка над полем ввода для включения/выключения анимации иконки в левом верхнем блоке.

16. AI-агент: микроправки позиционирования и ширины по фидбеку.
   - Поле `Введите вопрос...` расширено еще на `2px` с каждой стороны (`marginHorizontal: -2`).
   - Кнопка `Помощь PELBY` опущена с верхнего края и выровнена по визуальному положению шапок (через `paddingTop` контента и равные боковые отступы).

17. AI-агент: тюнинг анимации иконки в режиме «раздумия».
   - Вращение замедлено примерно в `4.5x` (`duration: 1600 -> 7600`).
   - Пульсация усилена (`scale: 1.06 -> 1.16`) и чуть ускорена для более выразимого эффекта.

18. AI-агент: точечная правка поля ввода по фидбеку.
   - Поле `Введите вопрос...` расширено еще на `3px` с каждой стороны (`marginHorizontal: -5`).
   - Поле опущено ниже на `3px` (`transform: translateY(3)`).

19. AI-агент: удаление тестовой кнопки и точное выравнивание кнопки помощи.
   - Удалена тестовая кнопка `Тест анимации` из-под блока подсказок.
   - Кнопка `Помощь PELBY` приведена к позиционированию шапок по тем же параметрам контейнера:
     `marginTop: 48`, `paddingVertical: 16`, `marginLeft: 12`, `marginRight: 16`, `marginBottom: 24`.

20. AI-агент: донастройка выравнивания по последнему фидбеку.
   - Поле `Введите вопрос...`: убран правый визуальный зазор за счет асимметричного расширения
     (`marginLeft: -5`, `marginRight: -8`) с выходом чуть за экран справа.
   - Кнопка `Помощь PELBY`: добавлен `paddingRight: 0` у контейнера шапки для точного совпадения с паттерном.
   - Иконка, welcome-заголовки и блок подсказок подняты выше примерно на `40px`
     (`translateY: -40` у `welcomeBlock` и `suggestionsBlock`), остальная раскладка не изменена.

21. AI-агент: устранение обрезания верхней иконки и «плашки» под кнопкой помощи.
   - Контейнер `Помощь PELBY` переведен в абсолютный слой (`position: absolute`), чтобы не занимать отдельную строку в раскладке.
   - Убрано обрезание верхней иконки: поднятие контента реализовано через `paddingTop` у `ScrollView`, без отрицательных трансформаций блока.

22. AI-агент: дополнительное расширение поля ввода вправо.
   - Для явного выхода за экран справа увеличен правый отступ поля:
     `marginRight: -14` (при `marginLeft: -5`).

23. AI-агент: жёсткая фиксация ширины поля ввода для правого выноса.
   - Поле `Введите вопрос...` переведено на явную ширину:
     `width: SCREEN_WIDTH + 19` и `alignSelf: 'flex-start'`.
   - Слева оставлен прежний вынос `marginLeft: -5`, справа маргин обнулен (`marginRight: 0`),
     чтобы правый край гарантированно заходил за экран.

24. AI-агент: корректировка позиции кнопки помощи и ссылки на `Help`.
   - Поле ввода сужено справа на `10px`: `width: SCREEN_WIDTH + 19 -> SCREEN_WIDTH + 9`.
   - Кнопка `Помощь PELBY` опущена до шапочного визуального уровня (`top: 64`).
   - Добавлен переход по кнопке на экран помощи: в текущем стеке `navigate('Help')`,
     иначе переход через таб `Диагностика` с `screen: 'Help'`.

25. AI-агент: исправлен сценарий возврата из `Help` по стрелке назад.
   - Для вкладки `AI-агент` добавлен отдельный стек `AIAssistantStack` (`AIAssistantMain` + `Help`).
   - Теперь при открытии `Help` из `AI-агент` кнопка «назад» возвращает на этот же экран `AI-агент`,
     а не в другой таб/стек.

26. AI-агент: разблокирована логика подсказок с ответами в стиле чата.
   - Подсказки снова кликабельны: при нажатии выбранная подсказка уходит вверх в историю диалога.
   - Выбранная подсказка меняет стиль: фон `rgba(25, 27, 223, 0.10)`, текст `#191BDF`.
   - Добавлен ответный блок под вопросом: фон `#E5EBF0`, текст `#20232D`,
     размер шрифта как у белого текста в подсказках (`17/22`, `300`).
   - Для первой подсказки зафиксирован постоянный ответ по ТЗ.
   - Для двух остальных подсказок добавлены базовые ответы по контексту Horeca-диагностики.
   - После выбора подсказки ниже остаются только неиспользованные подсказки.

27. AI-агент: дополнительная подгонка `Помощь PELBY` под шапки.
   - Убрана фиксированная ширина кнопки (в шапках кнопка автоширины).
   - Контейнер кнопки переведен на шапочный вертикальный паттерн: `top: 48` + `paddingVertical: 16`
     для совпадения посадки с существующими хедерами.

28. AI-агент: финальная калибровка вертикальной посадки `Помощь PELBY`.
   - Для полного совпадения с хедерами задана шапочная высота строки `topRow: height 88`
     (как строка с аватаром в других экранах), кнопка центрируется внутри этой высоты.

29. AI-агент: шапка переведена в поток экрана, чат опущен под неё.
   - Убрано абсолютное позиционирование шапочного блока на AI-экране.
   - Применен шапочный набор отступов из экранов приложения (`marginTop`, `marginBottom`, `marginLeft/Right`, `paddingVertical`).
   - `ScrollView` контент начинается сразу под шапкой (`paddingTop: 0`), поэтому чат и иконка «раздумий»
     больше не заезжают вверх под кнопку `Помощь PELBY`.

30. AI-агент: на экран поставлена полная общая шапка из приложения.
   - Локальная кастомная верхняя часть удалена.
   - Подключен общий `DashboardHeader` (лого/проект/город/кнопка `Помощь PELBY`) без изменений стилей.
   - Контент чата и иконка «раздумий» теперь всегда начинаются под этой шапкой.

31. AI-агент: закрепление иконки «раздумий» под шапкой.
   - Иконка вынесена из `ScrollView` в отдельный фиксированный блок сразу под общей шапкой.
   - Размер иконки уменьшен (`72x72`) для компактного постоянного индикатора.
   - Текст чата теперь прокручивается только под этим блоком, не заезжая выше него.

32. AI-агент: возвращена тестовая кнопка управления анимацией.
   - Восстановлен переключаемый стейт `isThinkingMock` для ручного включения/выключения анимации.
   - Возвращена кнопка `Тест анимации: включен/выключен` над полем ввода.

33. AI-агент: шапка с заменой проекта на иконку «раздумий».
   - `DashboardHeader` расширен пропсом `leftContent` для подмены левой части шапки без изменения кнопки `Помощь PELBY`.
   - На экране `AI-агент` в `leftContent` передана анимированная иконка «раздумий` вместо блока проекта
     (лого/название/город).
   - Отдельный блок иконки под шапкой удален: иконка теперь встроена прямо в шапку.

34. AI-агент: дополнительное увеличение иконки «раздумий».
   - Иконка в шапке увеличена еще на `+5` (`77x77 -> 82x82`).
   - Контейнер и fallback размер подогнаны под новый размер.

35. AI-агент: хирургическая правка нижней границы шапки.
   - Для компактного режима шапки (используется на AI-экране) нижняя граница поднята на `5px`:
     `marginBottom: spacing.sm -> spacing.sm - 5`.

36. AI-агент: дополнительное поднятие нижней границы шапки.
   - Нижняя граница в compact-режиме поднята еще на `10px`:
     `marginBottom: spacing.sm - 5 -> spacing.sm - 15`.

37. AI-агент: анимация «раздумий» привязана к фазам ответа.
   - Введен рабочий флаг `isAiBusy` вместо тестового статичного значения.
   - При нажатии на подсказку анимация включается на фазе «думает» (пауза перед ответом).
   - Затем анимация остается активной во время посимвольной «печати» ответа.
   - После завершения печати анимация выключается автоматически.
   - На время генерации отключаются повторные нажатия по подсказкам.

38. AI-агент: подключение экрана к реальному AI API.
   - Подсказки и ручной ввод теперь отправляются в `POST /ai/chat` через `askAiAssistant`.
   - Добавлен сбор истории диалога для контекста запроса (`user/assistant` последние сообщения).
   - Сохранена текущая UX-механика: индикатор «раздумий» активен во время ожидания и печати ответа.
   - Добавлены дружелюбные тексты ошибок (нет сети, провайдер не настроен, недоступен сервер).
   - Проверен продовый backend: `GET /health` отвечает `200`, но `POST /ai/chat` сейчас `404` (нужен деплой backend-ветки с AI-роутом).

39. Создан полный локальный бекап проекта.
   - Архив: `backup_20260306_184232_FULL_LOCAL_BEKAP.tar.gz`.
   - Размер: ~`100MB`.

40. Backend AI-обновление отправлено в GitHub.
   - Коммит: `84e627f` (`backend: add AI chat endpoint with yandex/gigachat providers`).
   - Push в `origin/main` выполнен успешно.
   - Внешняя проверка Render после пуша: `GET /health = 200`, `POST /ai/chat = 404` (прод пока на старой сборке, нужен запуск/проверка деплоя в панели Render).

41. Продовый backend после ручного деплоя подтвержден.
   - `GET /health = 200`.
   - `POST /ai/chat = 503` с `ai_provider_not_configured`.

42. Профиль: в блок `Документы` добавлен третий пункт `Согласие на обработку персональных данных`.
   - Карточка добавлена в том же стиле, что `Политика конфиденциальности` и `Условия использования`.
   - По нажатию открывается экран `PersonalDataPolicy` из профиля.
   - Возврат работает в тот же экран профиля (через back/goBack).
   - Вывод: роут AI задеплоен корректно, осталось добавить ключи провайдера в env Render.

42. Регистрация 1/3: подключен экран политики персональных данных.
   - Добавлен отдельный экран `[PersonalDataPolicyScreen]` с вашим визуальным стилем: фон `#F6F8FA`, скролл текста, анимируемая кнопка возврата.
   - В шаге `1/3` ссылка `персональных данных` теперь открывает этот новый экран вместо простого текста.
   - Текст политики перенесен полностью с `https://pelby.ru/policy` и локально вшит в приложение.
   - Во всех формулировках, где был указан сайт, заменено на приложение по запросу.

43. Правки экрана политики и обязательности согласия.
   - Заголовок экрана заменен на `Согласие на обработку персональных данных`.
   - Чекбокс согласия на шаге `1/3` сделан обязательным для прохода дальше.
   - Добавлена проверка кнопки `Пропустить`: при отсутствии галочки переход блокируется и показывается ошибка согласия.

44. Актуализирована логика согласия по сценариям заполнения данных.
   - Кнопка `Пропустить` на шаге `1/3` снова работает без обязательного согласия (если пользователь не вводит персональные данные).
   - При вводе/сохранении персональных данных согласие остается обязательным.
   - На экране `Редактировать профиль` добавлен чекбокс согласия и ссылка на документ; без согласия сохранение профиля заблокировано.

45. Экран согласия: добавлена кнопка `Согласен` и сценарий перехода.
   - Внизу экрана `Согласие на обработку персональных данных` добавлена синяя кнопка в стиле `Продолжить` из шага `1/3`.
   - По нажатию `Согласен` согласие сохраняется локально (`registrationStep1.consent = true`).
   - Если экран открыт из шага `1/3`, по `Согласен` пользователь сразу переводится на шаг `2/3`.
   - Если экран открыт из `Редактировать профиль`, по `Согласен` выполняется возврат назад, и чекбокс согласия в профиле автоматически подтягивается.

46. Кнопка `Согласен` перенесена под текст политики (не фиксированная поверх экрана).
   - Убрано закрепление кнопки поверх интерфейса.
   - Кнопка стала частью скроллируемого контента и располагается в самом низу документа.

47. Согласие в формах: отключена автопредустановка галочки при заполнении данных.
   - На шаге `1/3` удалено автозаполнение `consent` из сохраненных данных `registrationStep1`.
   - В `Редактировать профиль` убрана автоподтяжка согласия из `registrationStep1` и профиля пользователя.
   - Сохранен явный сценарий: если в документе нажать `Согласен`, при возврате в профиль чекбокс выставляется автоматически.

48. Стартовый роутинг: после первой успешной регистрации повторный вход ведет сразу в приложение.
   - В `AppWrapper` добавлен выбор стартового маршрута после `Splash`.
   - Если есть `authToken + userId + isAuthenticated=true`, выполняется `reset` на `MainTabs`.
   - Если пользователь не авторизован, но онбординг уже просмотрен (`hasSeenOnboarding=true`), открывается `Register1`.
   - Если данных нет, открывается `Onboarding1`.

49. Офлайн-сценарий: усилена фоновая синхронизация локальных данных на сервер.
   - В `syncService` добавлен хеш-снапшот локальных данных (`sync_last_snapshot_hash_v1`) для определения несинхронизированных изменений.
   - `pushLocalDataToServer` теперь отправляет данные только при изменениях (или `force=true`) и после успеха фиксирует хеш/время синка.
   - После `pullServerDataToLocal` локальный снапшот сразу помечается синхронизированным.
   - В `AppWrapper` добавлен автосинк: при старте приложения, при возврате в активное состояние и по таймеру каждые `45с`.

50. Регистрация: добавлена верификация телефона через `SMS/звонок` (только на шаге `1/3`).
   - Перед `Продолжить` теперь требуется подтверждение кода телефона на `RegisterScreen1`.
   - Добавлена модалка верификации с выбором канала (`SMS` / `Звонок`), вводом кода и повторной отправкой.
   - При смене номера флаг подтверждения сбрасывается; в `Редактировать профиль` логика не затрагивалась.

51. Профиль: добавлена временная тест-кнопка для проверки сценария регистрации.
   - На экране `Профиль` добавлена кнопка `Тест: открыть шаг 1/3`.
   - Кнопка ведет на экран `Register1` для быстрого перехода в поток проверки.

52. Регистрация: запуск верификации телефона вынесен на отдельную оранжевую подпись под полем номера.
   - Текст `Требуется подтверждение телефона` заменен на кликабельный `подтвердить номер`.
   - Цвет подписи переведен на фирменный оранжевый (`palette.primaryOrange`).
   - По нажатию на подпись запускается сценарий верификации (открывается модалка подтверждения).

53. Регистрация: после успешной проверки телефона обновлен статус под полем номера.
   - Вместо подписи отображается `верифицирован` зеленым цветом (`palette.success`).
   - Рядом добавлена маленькая зеленая галочка в одной строке.
   - Стиль `подтвердить номер` подогнан под типографику предупреждения под согласием (малый размер/вес/ритм).

54. Регистрация: финальная правка копирайта/стиля статуса верификации телефона.
   - Текст до верификации изменен на `Требуется верификация`.
   - Цвет текста до верификации изменен на красный (`#FF4D57`) в стиле предупреждения.
   - Текст после подтверждения изменен на `Верифицирован` (с заглавной буквы).

55. Backend: добавлены реальные эндпоинты верификации телефона через `SMS.ru`.
   - Добавлены маршруты `POST /auth/phone/start` и `POST /auth/phone/check`.
   - Поддержаны каналы `sms` и `call` (`callcheck`) с TTL, лимитом попыток и базовым rate-limit.
   - Добавлена нормализация RU-номеров (`+7` / `8` / `10 цифр`) и маскирование номера в ответе.
   - Расширен `backend/.env.example` настройками `SMSRU_*` и параметрами лимитов верификации.

56. Frontend (шаг `1/3`): модалка верификации переведена с моков на backend API.
   - Убрана тестовая генерация кода/`Alert`, подключены реальные запросы к `/auth/phone/start` и `/auth/phone/check`.
   - Для `Звонок` добавлен отдельный режим в модалке: инструкция + номер для звонка + проверка статуса.
   - Добавлена обработка серверных ошибок/лимитов и сетевых сбоев, блокировка кнопок на время запроса.

57. Backend: сессии телефонной верификации перенесены в БД.
   - Убрано хранение сессий и rate-limit в памяти процесса (`Map`), логика переведена на PostgreSQL.
   - Добавлена таблица `PhoneVerificationSession` и индексы через `ensureSchema`.
   - Эндпоинты `/auth/phone/start` и `/auth/phone/check` теперь читают/обновляют сессии в БД.
   - Поведение стало устойчивым к рестартам backend-инстанса.

58. Backend/Frontend phone verification изменения отправлены в GitHub.
   - Коммит: `88e284c` (`feat(auth): add real phone verification via SMS.ru`).
   - В `main` запушены: `backend/src/index.js`, `backend/.env.example`, `src/screens/RegisterScreen1.tsx`, `COMPLETED_TASKS_LOG.md`.

59. Продовая проверка после пуша выполнена.
   - `GET /health` отвечает `200 OK`.
   - `POST /auth/phone/start` и `POST /auth/phone/check` пока отвечают `404 Cannot POST`.
   - Это означает, что Render еще не поднял ревизию с коммитом `88e284c` (нужен ручной deploy latest commit, если auto-deploy отключен/завис).

60. Продовый деплой backend подтвержден после пуша.
   - `GET /health` возвращает `200 OK`.
   - Новые роуты `POST /auth/phone/start` и `POST /auth/phone/check` активны (больше нет `Cannot POST`).

61. Проверка сценариев phone verification на проде.
   - `POST /auth/phone/start` с `method=call` успешно создает сессию и возвращает `verificationId`/`callPhone`.
   - `POST /auth/phone/check` для call-сессии возвращает `202 verified=false` до факта подтвержденного звонка (ожидаемое поведение).
   - `POST /auth/phone/start` с `method=sms` на тестовом номере сейчас дает `500 server_error` (нужно проверить провайдерный ответ SMS.ru в Render logs/на реальном номере).

62. Регистрация (шаг 1/3): обновлен UX верификации телефона по правкам.
   - Ссылка `Верифицировать` перенесена вправо над полем `Телефон` и стилизована как `Пропустить`.
   - Сообщение `Требуется верификация` переведено в режим ошибки и показывается только при попытке `Продолжить` с неподтвержденным номером.
   - В ошибке оставлена кликабельная ссылка `Верифицировать` для запуска модалки.

63. Модалка верификации: скорректированы поведение и порядок действий.
   - Кнопки поменяны местами: `Отправить код повторно` теперь выше, `Подтвердить` — ниже.
   - Плейсхолдер `Введите код` теперь показывается только при пустом поле ввода.
   - Повторная отправка ограничена таймером: не чаще одного раза в `60` секунд.

64. Регистрация (шаг 1/3): доработка управления кнопкой `Верифицировать`.
   - Кнопка выровнена по одной высоте с заголовком `Телефон`.
   - Кнопка блокируется до ввода полного номера (`10` цифр после `+7`) и визуально приглушается.

65. Модалка верификации: убрано автопоявление ошибок после открытия.
   - Удалена автоотправка кода при открытии модалки, чтобы ошибка не появлялась сама.
   - Отправка выполняется только по явному действию пользователя (`Отправить код`).
   - При переключении `SMS/Звонок` сбрасывается текущая сессия/код/таймер для чистого сценария.

66. Модалка верификации переведена в SMS-only режим.
   - Удален подзаголовок `Выберите способ получения кода`.
   - Убран весь выбор метода (`SMS/Звонок`) и call-сценарий из UI шага `1/3`.
   - Подтверждение теперь всегда по SMS-коду (`Подтвердить`).
   - Кнопка отправки работает как `Отправить код` / `Отправить код повторно` с уже настроенным таймером 60 сек.

67. Модалка SMS-верификации: защита от ложного подтверждения без созданной сессии.
   - Кнопка `Подтвердить` теперь неактивна, пока backend не вернул `verificationId` (код не отправлен).
   - Убран сценарий, когда после падения `Отправить код` можно нажать `Подтвердить` и получить `Сначала запросите код`.

68. Диагностика причины падения SMS-верификации на проде.
   - По Render logs подтверждена ошибка SMS.ru `status_code=221`: не настроен/не подтвержден буквенный отправитель.
   - Это инфраструктурная причина, не проблема API-ключа и не проблема баланса.

69. Backend/Frontend: улучшена обработка ошибок SMS.ru.
   - Backend `auth/phone/start` теперь возвращает явные ошибки `sms_sender_not_approved` и `sms_provider_error` вместо общего `server_error`.
   - Добавлен env `SMSRU_FROM` (одобренный отправитель SMS.ru) в `backend/.env.example`.
   - Frontend `RegisterScreen1` показывает понятный текст при `sms_sender_not_approved`.

70. Регистрация (шаг 1/3): упрощен текст ошибки верификации телефона.
   - Под `Требуется верификация` удалена дополнительная ссылка `Верифицировать`.
   - Ссылка на верификацию оставлена только в правой части заголовка поля `Телефон`.

71. Регистрация (шаг 1/3): синхронный показ ошибок при нажатии `Продолжить`.
   - Ошибка `Требуется верификация` перенесена в общий `validateForm`.
   - Теперь ошибки по согласию и верификации телефона показываются одновременно, а не поэтапно.

72. Адресные поля (шаг `2/3`): исправлен выход из ввода по тапу мимо.
   - В `RegisterScreen2` упрощен dismiss клавиатуры/фокуса: тап мимо поля теперь стабильно закрывает ввод.
   - Для ScrollView применены параметры `keyboardShouldPersistTaps='handled'` и `keyboardDismissMode='on-drag'`.

73. Адрес из карты: усилено применение выбранного адреса.
   - В `RegisterScreen2` адрес из `MapPicker` теперь применяется только если непустой, после чего очищаются подсказки/ошибки и закрывается фокус.
   - Дополнительно закрывается клавиатура после возврата с карты.

74. Те же поведенческие правки адреса распространены на экраны заведений.
   - `AddProjectScreen` и `EditProjectScreen`: при возврате с карты адрес подставляется только если непустой, фокус/клавиатура закрываются.
   - На этих экранах также включен корректный dismiss клавиатуры по тапу мимо/drag.

75. MapPicker: защита от пустого подтверждения адреса.
   - На `Продолжить` добавлена проверка: если адрес пустой, показывается предупреждение, возврат без адреса не выполняется.

76. MapPicker: исправлен выход из поля адреса по тапу вне инпута.
   - При открытой клавиатуре добавлен явный dismiss по тапу на карту (оверлей) и по тапу в область листа вне текстового поля.
   - Добавлен `returnKeyType='done'` + `onSubmitEditing` для выхода из ввода с клавиатуры.
   - Поскольку `MapPicker` общий, фикс применяется во всех местах (`RegisterScreen2`, `AddProject`, `EditProject`).

77. RegisterScreen2: исправлен регресс фокуса поля адреса.
   - Восстановлены защитные проверки для dismiss (тап внутри инпута/подсказок больше не закрывает фокус).
   - Поле адреса теперь открывается и остается в фокусе, закрытие происходит только по тапу мимо.

78. Адресные подсказки: исправлено подставление города при выборе из dropdown.
   - Для подсказок Yandex Suggest добавлена нормализация значения: если есть `subtitle`, в поле подставляется полный адрес в формате `subtitle, title`.
   - Исправление применено в `RegisterScreen2`, `AddProjectScreen`, `EditProjectScreen`.
   - Убрана ситуация, когда в поле оставалась только улица и валидация требовала город.

79. Город в шапке: добавлен безопасный парсинг из адреса.
   - Вынесен общий helper `getCityFromAddress` в `src/utils/address.ts`.
   - Добавлено правило: если первая часть адреса — `Россия`/`Russian Federation`, город берется из следующей части.
   - Helper подключен в `DashboardHeader`, `DashboardScreen`, `ActionPlanScreen`, `SelfDiagnosisBlocksScreen`, `DiagnosisHistoryScreen`, `RegisterScreen3`, `ProfileScreen`.
   - Благодаря этому город в шапках/списках проектов корректно определяется и при адресах вида `Россия, Москва, ...`.

80. Диагностика: снижена анимация нажатия карточек ответов, чтобы убрать выход за края экрана.
   - В `AnimatedPressable` добавлены настраиваемые параметры анимации (`pressScale`, `pressFriction`, `pressTension`, `clampOvershoot`).
   - Для карточек ответов в `DiagnosisQuestionsScreen` включен мягкий режим (`pressScale=0.97`, `clampOvershoot=true`).
   - Визуальный эффект нажатия сохранен, но без мигания контуров и вылета за границы экрана.

81. Профиль: удалена временная тестовая кнопка перехода на регистрацию `Тест: открыть шаг 1/3`.
   - Удален блок кнопки из `ProfileScreen`.
   - Удалены связанные стили `registerStepTestButton` и `registerStepTestButtonText`.

82. Экран задач: усилена стабильность сворачивания/разворачивания блока.
   - В `ActionPlanScreen` исправлена сортировка завершенных блоков без мутации state-массива (`[...completedBlocks]` перед `sort`).
   - Обработчик `toggleBlockCollapse` переведен на `useCallback` и безопасное булево переключение состояния.
   - Для заголовков блоков добавлен `hitSlop`, чтобы нажатие по шапке срабатывало стабильнее.

83. Экран задач (`ActionPlan`): дополнительный фикс тапа по заголовку блока.
   - Заголовки блоков переведены с `Pressable` на `AnimatedPressable` (внутри `TouchableOpacity`) для более стабильного срабатывания в вертикальном `ScrollView`.
   - На обе шапки блоков (пройденные/непройденные) оставлен `hitSlop=8` и мягкая анимация нажатия.
   - У `blocksDivider` отключен перехват касаний (`pointerEvents: 'none'`), чтобы линия не мешала тапам по соседним элементам.

84. Экран задач: фикс кейса, когда у «пройденного» блока визуально не происходит раскрытие.
   - Добавлена нормализация `blockId` для загруженных задач (поддержка legacy-идентификаторов/названий блока).
   - Нормализация применяется в обоих потоках загрузки задач (`loadTasksWithoutClearing`, `loadTasks`).
   - Для раскрытого завершенного блока без задач добавлен явный пустой стейт: `По этому блоку задачи пока не сформированы`.

85. Корневой фикс потери задач по завершенному блоку в новом флоу `DiagnosisQuestions -> BlockResults`.
   - Выявлено: в этом флоу при завершении блока не выполнялось сохранение задач в `actionPlanTasks` (в отличие от legacy `BlockQuestions`).
   - В `AppWrapper` добавлена синхронизация завершенного блока `syncCompletedBlockData(blockId)`:
     - пересчет эффективности блока по фактическим ответам;
     - обновление `diagnosisBlocks` (completed/efficiency/answers);
     - пересборка задач именно этого блока через `generateTasksFromAnswers` и атомарная замена задач блока в `actionPlanTasks`.
   - Синхронизация вызывается:
     - при `onBlockComplete` из `DiagnosisQuestions` перед переходом в `BlockResults`;
     - в `NextBlock`, если блок уже отвечен и открывается `BlockResults` напрямую.
   - Автодогенерация/фолбэки «из воздуха» не используются: задачи строятся только из сохраненных ответов блока.

86. Данные задач/блоков синхронизированы в новом флоу прохождения блока.
   - Исправлен `AppWrapper`: при завершении блока через `DiagnosisQuestions` теперь обязательно сохраняются:
     - актуальный статус блока в `diagnosisBlocks` (completed/efficiency/answers),
     - задачи этого блока в `actionPlanTasks` (пересборка строго из ответов блока, без фолбэк-задач).
   - Добавлен тот же вызов синхронизации при прямом переходе `NextBlock -> BlockResults`, если блок уже полностью отвечен.

87. Ремонт существующих данных на экране `ActionPlan`.
   - Если в хранилище уже есть задачи, но для некоторых завершенных блоков задач нет,
     `ActionPlan` дополняет только отсутствующие блоки задачами, собранными из сохраненных ответов.
   - Фолбэк-задачи не создаются: берутся только задачи, реально сгенерированные из ответов пользователей.

88. Дашборд: логика строки «Прошлая диагностика» приведена к фактическому смыслу без изменения стилей.
   - В бейдже «Прошлая диагностика» теперь отображается `comparison.previous` (прошлый результат), а не текущий `overallEfficiency`.
   - Если предыдущего результата нет, выводится `—` в нейтральном сером бейдже.
   - Индикатор дельты (`▴/▾` и `%`) оставлен без стилистических изменений и показывается только при наличии `previous + current`.

89. Профиль: добавлена временная тестовая кнопка перехода на экран «Диагностика завершена» (с заметками).
   - В `ProfileScreen` добавлен обработчик `handleOpenDiagnosisResultsTest`.
   - Кнопка `Тест: Диагностика завершена` добавлена в блок `Остальное`.
   - Навигация выполняется в корневой роут `DiagnosisResults` через parent navigation, чтобы экран открывался из таба `Профиль`.

90. Профиль: удалена временная тестовая кнопка `Тест: Диагностика завершена`.
   - Из `ProfileScreen` удалены кнопка и связанный обработчик `handleOpenDiagnosisResultsTest`.
   - Удалены связанные стили `diagnosisCompletedTestButton` и `diagnosisCompletedTestButtonText`.

91. SMS.ru верификация (шаг `1/3`): добавлена явная проверка отправителя на backend.
   - В `POST /auth/phone/start` добавлена ошибка `sms_sender_not_configured`, если не задан `SMSRU_FROM`.
   - На фронтенде `RegisterScreen1` добавлен отдельный текст для `sms_sender_not_configured`.
   - Уточнен текст для `sms_sender_not_approved` (проверка соответствия одобренного отправителя и `SMSRU_FROM`).
   - В `backend/.env.example` обновлена подсказка для `SMSRU_FROM` (рекомендовано явно задавать в проде).

92. SMS.ru: внесены боевые параметры отправителя для проверки верификации телефона.
   - В локальный `backend/.env` добавлены `SMSRU_API_ID` и `SMSRU_FROM=OOO Pelby` по предоставленным данным.
   - Подготовлен следующий шаг: синхронно выставить те же значения в env продового backend (Render) и перезапустить деплой.

93. Профиль: возвращена тестовая кнопка для перехода на регистрацию `1/3`.
   - В `ProfileScreen` добавлен обработчик `handleOpenRegisterStep1Test`.
   - Кнопка `Тест: открыть шаг 1/3` добавлена в блок `Остальное`.
   - Переход реализован в корневой роут `Register1` через `parent navigation`, чтобы из таба `Профиль` открывался экран шага `1/3`.

94. Регистрация `1/3`: исправлен сценарий верификации телефона после ввода SMS-кода.
   - Убран автопереход на экран `2/3` сразу после успешного `Подтвердить` в модалке верификации.
   - После подтверждения пользователь остается на шаге `1/3` и видит статус `Верифицирован`.
   - В `registrationStep1` добавлено сохранение `phoneVerified` и `verifiedPhoneDigits`.
   - При повторном открытии шага `1/3` статус верификации восстанавливается из сохраненных данных, если номер не изменился.

95. SMS-верификация: уточнена обработка ошибки `status_code=201` от SMS.ru.
   - В backend добавлен отдельный код `sms_balance_insufficient` при ответе SMS.ru `Не хватает средств на лицевом счету`.
   - В `POST /auth/phone/start` этот кейс возвращается явно (`error: sms_balance_insufficient`).
   - На шаге `1/3` в `RegisterScreen1` добавлен понятный текст: требуется пополнить баланс SMS.ru.

96. SMS-верификация: скрыта служебная причина ошибки от пользователя.
   - Для кейса `sms_balance_insufficient` backend снова возвращает общий `sms_provider_error` наружу.
   - Детализация по причине остается только в backend-логах, UI показывает общий текст без информации о балансе.

97. Профиль: повторно удалена тестовая кнопка перехода на регистрацию `Тест: открыть шаг 1/3`.
   - Из `ProfileScreen` удалены кнопка, обработчик `handleOpenRegisterStep1Test` и связанные стили.

98. Регистрация `1/3`: закреплена логика «верификация телефона один раз для пользователя».
   - В `usersStorage` добавлены поля пользователя: `phoneVerified`, `verifiedPhoneDigits`, `phoneVerifiedAt`.
   - В `RegisterScreen1` статус верификации теперь сохраняется не только в `registrationStep1`, но и в записи пользователя.
   - При повторном открытии шага `1/3` статус верификации восстанавливается по email/телефону из сохраненной записи пользователя.
   - Если номер телефона совпадает с ранее верифицированным для этого пользователя, повторная SMS-верификация не требуется.

99. Диагностика блока: кнопка `Смотреть задачи` теперь открывает задачи конкретно этого блока.
   - В `AppWrapper` для экрана `BlockResults` переход ведет в `MainTabs -> Задачи -> ActionPlanMain` с параметрами:
     `selectedTab=blockId`, `targetBlockId=blockId`, `jumpNonce`.
   - Благодаря этому в `ActionPlan` автоматически активируется нужный таб блока и выполняется прокрутка к его карточке.
   - Аналогичный переход с параметрами применен для `DashboardBlockResults` в `AppNavigator`.

100. Фоновая синхронизация: добавлена устойчивость к сетевым сбоям `fetch`.
   - В `syncService` добавлены timeout (`15s`) и один автоматический retry для `/sync/push` и `/sync/pull`.
   - Сетевые ошибки (`Network request failed`/timeout/abort) больше не логируются как `ERROR` на каждый цикл.
   - Для сетевых фейлов добавлен throttled `warn` (не чаще 1 раза в минуту), чтобы убрать шум в логах при временной недоступности сети/бэкенда.

101. Фоновая синхронизация: расширена классификация сетевых ошибок и добавлен backoff.
   - В `syncService` распознавание сетевых ошибок усилено для RN/whatwg-fetch (`fetch.umd.js`, `request failed`, `xmlhttprequest`, `timeout`).
   - После сетевого фейла включается backoff на 2 минуты: новые попытки `push/pull` временно пропускаются, чтобы не засыпать лог ошибками.
   - При успешной синхронизации backoff автоматически сбрасывается.

102. Глобально сглажено открытие модалок/попапов.
   - Bottom-sheet и полуэкранные модалки переведены на `animationType="slide"`: `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks`, `DiagnosisResults`, `DiagnosisHistory`, `TaskSubtasks`, `ErrorBottomModal`.
   - Центровые диалоги (confirm/login/date/menu/верификация) оставлены на `fade`, чтобы не ломать UX.
   - Для модалок добавлен `statusBarTranslucent`, чтобы оверлей/анимация открывались плавнее на Android.

103. Откат модалок до предыдущего поведения (локально, без деплоя).
   - Все изменения по плавной анимации отменены: `animationType="slide"` возвращен на `fade`.
   - Удалены добавленные `statusBarTranslucent` во всех модалках, затронутых апдейтом.
   - Приложение возвращено к состоянию модалок «как до обновления анимации».

104. Модалки выбора проекта: удалена кнопка `Повторная диагностика`.
   - В модалках `Выберите ресторан` на экранах `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks` удалена кнопка `Повторная диагностика`.
   - В этих окнах оставлены только выбор проекта и действие `Добавить еще ресторан`.
   - Удалены неиспользуемые стили `repeatDiagnosisModalButton` и `repeatDiagnosisModalButtonText` в соответствующих экранах.

105. Модалки выбора проекта: выровнены вертикальные отступы после удаления кнопки.
   - В `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks` модалка выбора проекта переведена на авто-высоту с ограничением `maxHeight`, чтобы убрать пустой хвост снизу.
   - Отступы вокруг `+ Добавить еще ресторан` выровнены: сверху и снизу по 14px (`marginTop -10` при `venuesCard marginBottom 24` и `paddingBottom 14`).
   - Модалка визуально опускается ниже и выглядит компактнее при небольшом числе проектов.

106. Модалки выбора проекта: отступы дополнительно увеличены еще на 5px сверху и снизу.
   - В `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks` увеличен нижний отступ блока `+ Добавить еще ресторан` до `24px` (`paddingBottom: spacing.lg`).
   - Увеличен верхний отступ над `+ Добавить еще ресторан` до `24px` (`addRestaurantRow marginTop: 0` при `venuesCard marginBottom: spacing.lg`).

107. Модалки выбора проекта: `+ Добавить еще ресторан` поднят на 10px.
   - В `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks` изменен только `addRestaurantRow marginTop: 0 -> -10`.
   - Остальные отступы и размеры модалки не менялись.

108. Кнопка `+ Добавить еще ресторан` в модалках синхронизирована с плюсиком в профиле.
   - В модалках выбора проекта (`Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks`) кнопка теперь ведет в `AddProject`.
   - Маршрут полностью выровнен с логикой иконки `+` в `Profile` (`Мои заведения`).

109. Модалки выбора проекта: выровнены строки проектов и переключатели по высоте.
   - В `Dashboard`, `ActionPlan`, `SelfDiagnosisBlocks` добавлены `venueRow minHeight: 50`, `venueInfo justifyContent: 'center'`, `radioButton alignSelf: 'center'`.
   - В `DiagnosisHistory` добавлены `venueRow minHeight: 56`, `venueInfo justifyContent: 'center'`, центрирование `venueCheckPlaceholder`.
   - Визуально выровнена вертикаль: переключатель и текст проекта находятся на одной оси в каждой строке.

110. Нижнее меню: точечно уменьшена зона нажатия таба `Диагностика`.
   - В `AppNavigator` для таба `Диагностика` удалено искусственное расширение hit-area (`minWidth: 90` и дополнительный `paddingHorizontal`).
   - Оставлен только базовый `paddingHorizontal: 0`, чтобы зона тапа соответствовала стандартной ширине таба и не перехватывала нажатия по соседней вкладке `Задачи`.

111. Нижнее меню: откат регресса раскладки подписей табов.
   - Для таба `Диагностика` в `AppNavigator` возвращены исходные параметры `flexShrink`, `minWidth: 90` и `paddingHorizontal: 4`.
   - Это восстанавливает корректное расстояние между подписями в нижнем меню (без склеивания `Задачи/Диагностика/AI-агент`).

112. Нижнее меню: исправлен промах по табам без изменения визуала.
   - В `AppNavigator` сдвиги табов перенесены из `tabBarItemStyle` в `tabBarLabelStyle` и обертки `tabBarIcon`.
   - Внешний вид меню сохранен (иконки/подписи на тех же местах), но hit-area каждого таба осталась в своей стандартной колонке.
   - Убрана причина перехвата нажатий, когда тап по `Задачи` попадал в `Диагностика`.

113. Нижнее меню: полностью пересобрана равномерная сетка табов (5 колонок).
   - Удалены все ручные горизонтальные сдвиги (`translateX`) и индивидуальные ширины/растяжения табов.
   - Для всех табов применены единые стили: одинаковая ширина колонки, центрирование иконок/подписей, один размер шрифта подписи.
   - Отступы меню выровнены (`height/padding`), чтобы подписи отображались целиком без склеивания и сокращений.

114. Нижнее меню: точечная микро-коррекция позиционирования двух табов.
   - Таб `Задачи` (иконка+подпись) сдвинут влево на `3px`.
   - Таб `AI-агент` (иконка+подпись) сдвинут вправо на `3px`.
   - Изменение только визуальное, hit-area табов не менялась.

115. Нижнее меню: `AI-агент` дополнительно сдвинут вправо еще на `8px`.
   - Для таба `AI-агент` визуальный сдвиг обновлен с `+3px` до `+11px` (иконка + подпись).
   - Остальные табы и hit-area не менялись.

116. Нижнее меню: `AI-агент` сдвинут левее на `2px`.
   - Для таба `AI-агент` визуальный сдвиг обновлен с `+11px` до `+9px` (иконка + подпись).
   - Остальные табы и hit-area без изменений.

117. Нижнее меню: иконки и подписи подняты выше на `3px` без изменения высоты поля меню.
   - В `AppNavigator` для `fixedTabBarStyle` изменены только внутренние отступы: `paddingTop 6 -> 3`, `paddingBottom 8 -> 11`.
   - Высота таббара оставлена прежней (`height: 78`), остальные параметры меню не изменялись.

118. Плюсик на дашборде и в диагностике переведен на единый сценарий `Новая диагностика`.
   - Иконка `+` на `Dashboard` теперь открывает ту же модалку старта новой диагностики, что и кнопка `Новая диагностика`.
   - Иконка `+` на `SelfDiagnosisBlocks` также переведена на этот же сценарий (через общий `openNewDiagnosisModal`).
   - Промежуточная логика выбора проекта/добавления ресторана с `+` убрана: при закрытии модалки пользователь остается на экране-источнике (`Dashboard` или `Диагностика`).

119. Исправлен белый экран по кнопке `Продолжить диагностику`.
   - Причина: кнопка в `SelfDiagnosisBlocks` вела в `BlockQuestions`, а этот экран сейчас пустой (`return <View />`), из-за чего открывался белый экран.
   - Фикс: кнопка `Продолжить диагностику` переведена на актуальный сценарий `NextBlock` через `handleBlockPress(nextBlock)`.
   - Поведение теперь единое с тапом по карточке блока: продолжение идет по рабочему маршруту диагностики.

120. Исправлена рассинхронизация иконки проекта между профилем и шапкой.
   - В `DashboardHeader` источник данных расширен: теперь шапка читает проекты из `registrationStep2` и `user_questionnaireData` (как в профиле), а не только из questionnaire.
   - Добавлена нормализация логотипа по полям `logoUri / projectLogoUri / logo` и валидация пустых значений.
   - Для выбранного проекта в шапке теперь берется актуальный логотип, что выравнивает отображение после обновления иконки в профиле.

121. Нормализовано извлечение города из адреса: только город, иначе регион.
   - Обновлена `getCityFromAddress` в `src/utils/address.ts`.
   - Приоритет: сначала строго город (`г.` / `город` + федеральные города), если не найден — регион (`область/край/республика/округ`).
   - Убрано старое поведение "брать просто первый кусок адреса", из-за которого могли подтягиваться значения вроде "городской парк".

122. Доработано извлечение города: возвращены кейсы с обычным названием города без префикса.
   - В `getCityFromAddress` добавлен второй проход: если нет `г.`/`город`, выбирается "чистое" название города (без цифр и адресных/локационных слов).
   - Добавлены фильтры не-городских фрагментов (`городской парк`, улицы, дома, БЦ/ТЦ и т.д.), чтобы такие значения не попадали в поле города.
   - Логика приоритета сохранена: сначала город, если не найден — регион, иначе fallback.

123. Режим `100% эффективности` переведен на строгое условие.
   - В `DashboardScreen` критерий max-экрана изменен: теперь это только `все блоки завершены` и `каждый блок имеет 100%`.
   - Убрана привязка max-экрана к округленному среднему значению `overallEfficiency`.
   - Тестовый override сохранен отдельно (`forceMaxEfficiency`) только для debug-сценариев.

124. В профиль добавлена тестовая кнопка входа в спец-блок `100%`.
   - В разделе `Остальное` добавлена кнопка `Тест 100% эффективности`.
   - Кнопка ставит one-time debug-флаг и переводит пользователя на главную (`DashboardMain`) в max-preview.
   - Debug-preview автоматически сбрасывается при уходе с экрана дашборда.

125. Убрана белая окантовка вокруг плашки `100%` на max-экране.
   - В `DashboardScreen` удалены `borderWidth/borderColor` у `maxCheckBadge`.
   - Также удалена белая обводка у `addWarningBadge`, чтобы бейдж процента в модалке выглядел одинаково.
   - Геометрия/позиционирование бейджей не менялись.

126. Нижнее меню: убраны ручные горизонтальные сдвиги у `Задачи` и `AI-агент`.
   - В `AppNavigator` удалены `translateX` для иконки и подписи вкладки `Задачи`.
   - Удалены `translateX` для иконки и подписи вкладки `AI-агент`.
   - Возвращена ровная сетка таббара без точечных сдвигов этих двух вкладок.

127. Нижнее меню: микросдвиг только двух вкладок по запросу.
   - `Задачи`: иконка и подпись сдвинуты влево на `3px`.
   - `AI-агент`: иконка и подпись сдвинуты вправо на `3px`.
   - Остальные вкладки и параметры таббара не изменялись.

128. Нижнее меню: `AI-агент` дополнительно сдвинут вправо на 2px.
   - Сдвиг вкладки `AI-агент` обновлен с `+3px` до `+5px` для иконки и подписи.
   - Остальные вкладки и параметры меню без изменений.

129. Экран `100% эффективности`: обе кнопки привязаны к рабочим сценариям.
   - `Настроить уведомления` теперь ведет в `Профиль` и автоматически открывает ту же модалку уведомлений, что и кнопка `Уведомления` в профиле.
   - `Повторная диагностика` теперь использует тот же поток, что `+` на дашборде (`openNewDiagnosisModal` с предупреждающей модалкой по незавершенным задачам).
   - Реализован one-time флаг `profile_open_notifications_once` для надежного открытия модалки уведомлений при кросс-экранном переходе.

130. Экран `100% эффективности`: отдельный сценарий модалки для кнопки `Повторная диагностика`.
   - Заголовок модалки оставлен прежним: `Начать новую диагностику?`.
   - Текст заменен на `У вас не осталось невыполненных задач!` и окрашен в зеленый цвет (как в шкале эффективности).
   - Вместо двух кнопок в этом режиме остается одна: `Начать новую диагностику` со стилем текущей синей кнопки `К задачам`.
   - Обычный сценарий (когда есть незавершенные задачи) сохранен без изменений: две кнопки `Все равно начать диагностику` и `К задачам`.

131. Max-модалка `Повторная диагностика`: визуальная доводка кнопки и бейджа.
   - Для max-сценария кнопка `Начать новую диагностику` опущена ниже в модалке (увеличен верхний отступ только в этом режиме).
   - Бейдж у лого в max-сценарии теперь всегда показывает `100%` и соответствующий цвет, вместо текущего среднего процента.

132. Повторная диагностика: убран max-popup, переход сразу в `Шаг 3/3` и сохранение базы прошлой диагностики.
   - На экране `100%` кнопка `Повторная диагностика` теперь без модалки сразу переводит в `Register3` (шаг `3/3`, выбор проекта) без предварительного reset-вызова на дашборде.
   - Удален временный отдельный режим max-модалки (`У вас не осталось невыполненных задач!`) — возвращен единый warning-popup только для `+`/обычного сценария.
   - В `startRepeatDiagnosisForVenue` отключено удаление прошлых результатов/задач/ответов; сохраняются история и данные предыдущей диагностики для сравнения (`прошлая диагностика`, дельты по блокам, график истории).
   - При старте repeat очищаются только backup-ключи ответов и заново выставляются `diagnosis_progress`, `diagnosis_selected_blocks`, `diagnosis_repeat_mode=true`.
   - В `DashboardScreen` добавлен fallback: если ответов блока нет, но есть сохраненный результат блока, он сохраняется в отображении вместо обнуления (фикс для уже затронутых старым сценарием).

133. История/дашборд/нижнее меню: стабилизация после холодного перезапуска.
   - Удален деструктивный код в `DiagnosisHistoryScreen`, который обнулял `diagnosis_history`, если не находил ключи ответов по проекту.
   - В `DashboardScreen` для ветки `результат не изменился` добавлено `appendHistoryEntryIfChanged(...)`, чтобы история не оставалась пустой и при неизменной эффективности.
   - В `AppNavigator` стабилизована геометрия таббара: каждому табу задана фиксированная ширина (`20%`), а контейнеры иконок получили фиксированный размер `23x23`, чтобы убрать визуальный сдвиг на холодном старте.

134. Нижнее меню: откат последней геометрии таббара из-за регресса после перезапуска.
   - Из `AppNavigator` убраны фиксированные `width/maxWidth=20%` для `tabBarItemStyle`.
   - Убраны жесткие размеры контейнеров иконок (`23x23`) в табах.
   - Возвращена предыдущая схема таббара с пользовательскими микросдвигами: `Задачи -3px`, `AI-агент +5px`.

135. Нижнее меню: устранен скачок раскладки на холодном старте.
   - В `AppNavigator` добавлены стабильные слоты иконок таббара (`23x23`) для всех вкладок.
   - Это убирает визуальный сдвиг при смене fallback-иконок на SVG после загрузки ассетов.
   - Сохранены пользовательские смещения: `Задачи -3px`, `AI-агент +5px`; общая геометрия табов не менялась.

136. Нижнее меню: замена `transform` на статический `marginLeft` для `Задачи/AI-агент`.
   - В `AppNavigator` убраны `translateX` у иконки/подписи для табов `Задачи` и `AI-агент`.
   - Сдвиги сохранены в тех же значениях (`-3` и `+5`), но теперь через `marginLeft`, чтобы исключить нестабильный initial layout после cold start.
   - Для равномерного распределения табов добавлен `flexBasis: 0` в `tabBarItemStyle`.

137. Нижнее меню: полная фиксация равных расстояний между иконками.
   - Убраны все ручные горизонтальные сдвиги (`marginLeft/transform`) у `Задачи` и `AI-агент`.
   - Все 5 табов возвращены к единому центрированию в одинаковых слотах.
   - Стабильный слот иконки (`23x23`) сохранен для одинаковой геометрии до/после загрузки SVG.

138. Нижнее меню: заменен системный tab bar на кастомный с фиксированной сеткой.
   - В `AppNavigator` подключен кастомный `tabBar` с 5 равными кнопками (`flex: 1`, `flexBasis: 0`) и единым центрированием.
   - Расстояния между иконками теперь задаются только этой сеткой, без внутренних алгоритмов layout от стандартного tab bar.
   - Скрытие таббара на экранах `Help`/`TaskSubtasks` сохранено через анализ `tabBarStyle.display`.

139. Нижнее меню: принудительно выровнены интервалы между иконками.
   - В кастомном tab bar убран `justifyContent: space-between`.
   - Ряд табов переведен на `justifyContent: flex-start`.
   - Каждому табу задана строгая ширина `100 / countTabs %`, чтобы расстояние между центрами иконок было одинаковым.

140. Нижнее меню: окончательная фиксация равных слотов без влияния `flex`.
   - Из стиля таба убраны `flex/flexBasis`; оставлены только центрирование и вертикальные отступы.
   - Для каждой кнопки таба одновременно заданы `width/minWidth/maxWidth = 100 / countTabs %`.
   - Это исключает перераспределение ширины между вкладками и фиксирует одинаковые интервалы между центрами иконок.

141. Repeat-диагностика: убран перенос старых заполненных блоков в новый прогон.
   - В `startRepeatDiagnosisForVenue` при старте repeat снова очищаются `diagnosis_answers_*` и `diagnosisBlocks` (user/global) по текущему проекту.
   - История, прошлые результаты и метрики дашборда не очищаются, чтобы сохранялись `прошлая диагностика`, дельты и график истории.
   - Кнопка `Повторная диагностика` на `Dashboard` снова вызывает `startRepeatDiagnosisForVenue(...)` перед переходом на `Register3`.

142. Запуск новой диагностики через `+`: при `0` незавершенных задач модалка пропускается.
   - На `Dashboard` и `SelfDiagnosisBlocks` обновлен сценарий `openNewDiagnosisModal`: сначала считается число незавершенных задач.
   - Если задач `0`, сразу выполняется `startRepeatDiagnosisForVenue(...)` и переход на `Register3` (шаг `3/3`) без показа warning-модалки.
   - Если задач больше `0`, поведение прежнее: показывается модалка с кнопками `Все равно начать диагностику` / `К задачам`.

143. Дельта блоков в repeat-режиме: зафиксирован baseline прошлой диагностики.
   - При старте repeat в `startRepeatDiagnosisForVenue` сохраняется baseline-слепок эффективностей по блокам в ключ `diagnosis_repeat_baseline_block_efficiencies` (user/global).
   - В `DashboardScreen` расчет `previousBlockEfficiencies` в repeat-режиме теперь сначала берется из этого baseline-ключа, а не из плавающей истории.
   - При завершении repeat (`finalizeRepeatDiagnosisForVenue`) baseline-ключ очищается.

144. Диагностика: устранен ложный `0 задач` в завершенном блоке при первом входе.
   - В `SelfDiagnosisBlocksScreen` (`refreshIncompleteTasks`) добавлен fallback на сохраненные `diagnosisBlocks`, если `blockProgress` еще не успел заполниться.
   - Теперь completed-блоки для фильтра задач определяются корректно уже на первом рендере, без необходимости перехода в `Задачи` и обратно.

145. Профиль: удалена тестовая кнопка `Тест 100% эффективности`.
   - Удалена карточка из секции `Остальное` в `ProfileScreen`.
   - Удалены связанный хендлер `handleOpenMaxEfficiencyPreview` и debug-константа `DEBUG_FORCE_MAX_EFFICIENCY_ONCE_KEY` из `ProfileScreen`.
   - Для сохранения верстки секции стиль `settingsCardLast` перенесен на карточку `Удалить аккаунт`.

146. Подзадачи: добавлено рабочее меню по троеточию в правом верхнем углу.
   - Реализованы пункты: `Показывать выполненные`, `Сначала невыполненные`, `Очистить выполненные` (с подтверждением).
   - Удаление задачи через это меню не добавлялось (по согласованию), свайп-удаление остается только для подзадач.

147. Подзадачи: меню стилизовано под фильтр из дашборда.
   - Меню переведено в нижний `sheet`-формат с затемнением фона, заголовком и кнопкой закрытия.
   - Опции оформлены рядами в стиле `filterOptionRow` с теми же радиусами/рамками/индикаторами выбора.

148. Подзадачи: уточнены формулировки и поведение фильтра в меню.
   - Заголовок модалки изменен с `Настройки списка` на `Подзадачи`.
   - Опция изменена на `Скрыть выполненные` (активный индикатор теперь соответствует именно режиму скрытия).
   - Логика фильтра подтверждена: при активном режиме скрытия completed-подзадачи исключаются из `visibleSubtasks`.

149. Подзадачи: исправлено поведение отметки `выполнено` (не должны исчезать по умолчанию).
   - Введен явный флаг `hideCompletedSubtasks` (вместо инверсной логики `showCompletedSubtasks`) для исключения путаницы.
   - При открытии экрана подзадач режим скрытия сбрасывается в дефолт: выполненные показываются, как на экране задач.
   - Выполненные подзадачи скрываются только когда пользователь явно включает пункт `Скрыть выполненные`.

150. Профиль: добавлена фича удаления аватарки.
   - На аватаре в профиле добавлена маленькая красная иконка корзины (overlay в правом нижнем углу), видна только если фото уже установлено.
   - При нажатии открывается подтверждение `Удалить фото профиля?`.
   - После подтверждения очищаются ключи `userAvatar`, `profile_avatar_uri`, `user_${userId}_profile_avatar_uri`, и аватар сразу убирается из UI.

151. Бэкенд: добавлено хранение переписки с ИИ в БД + просмотр в админке.
   - В `backend/src/index.js` добавлена таблица `AiChatMessage` (создается в `ensureSchema`) с индексами по времени и `userId`.
   - В `POST /ai/chat` добавено логирование каждого запроса: текст пользователя, история, контекст, провайдер, ответ/ошибка.
   - Расширены админ-эндпоинты:
     - `GET /admin/users` теперь возвращает `aiMessagesCount` и `lastAiMessageAt`.
     - `GET /admin/user/:userId` теперь возвращает `aiTotalCount` и `aiChats` (с лимитом через `aiLimit`).
     - Добавлен `GET /admin/ai/chats` для общего списка AI-чатов (опционально фильтр `userId`, `limit`).
     - Добавлен `GET /admin/phone-verifications` для просмотра сессий верификации телефона (`phone`, `limit`).
   - В `backend/.env.example` добавлены новые параметры лимитов админ-выборок и лимит длины AI-логов.
